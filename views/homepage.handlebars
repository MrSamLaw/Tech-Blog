<!-- Loop over each object in the projects array -->
{{#each posts as |post| }}
<div class="jumbotron">
    <h2 class="display-4"><a href="/post/{{post.id}}">{{post.name}}</a></h2>
    <p class="lead">{{summary}}</p>
    <p>Author: {{user.name}} | {{format_date date_created}}</p>
</div>
{{/each}}